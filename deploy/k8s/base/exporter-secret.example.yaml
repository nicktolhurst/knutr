apiVersion: v1
kind: Secret
metadata:
  name: knutr-exporter-secrets
  namespace: knutr
  labels:
    app.kubernetes.io/name: knutr-plugin-exporter
    app.kubernetes.io/component: plugin
    app.kubernetes.io/part-of: knutr
type: Opaque
stringData:
  # Copy this file to exporter-secret.yaml and fill in your values.
  # exporter-secret.yaml is gitignored and will not be committed.
  postgres-password: "REPLACE-ME"
  Slack__BotToken: "xoxb-REPLACE-ME"
